## Project

- Criação de um setup kubernetes via `ansible`, `vagrant` e `virtualbox`.

## Execução

1. Instalar pré-requisitos

```bash
make requirements
```

2. Subir ambiente

```bash
vagrant up
```

3. Para ambiente

```bash
vagrant halt
```

4. Remover ambiente

```bash
vagrant destroy -f
```